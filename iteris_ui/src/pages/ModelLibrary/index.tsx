/**
 * Model library (spec §8) — placeholder scaffold.
 * Owned by the Library/Explorer agent: filterable model cards + training curves.
 */
export default function ModelLibrary() {
  return (
    <main className="min-h-screen bg-bg font-body text-text">
      <div className="mx-auto max-w-3xl px-6 py-16">
        <h1 className="font-heading text-3xl font-bold text-accent">
          Model Library
        </h1>
        <p className="mt-4 text-muted">Scaffold — built by the Library agent.</p>
      </div>
    </main>
  );
}

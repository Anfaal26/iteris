/**
 * Dataset explorer (spec §9) — placeholder scaffold.
 * Owned by the Library/Explorer agent: CAMUS + BRISC curated strips and grids.
 */
export default function DatasetExplorer() {
  return (
    <main className="min-h-screen bg-bg font-body text-text">
      <div className="mx-auto max-w-3xl px-6 py-16">
        <h1 className="font-heading text-3xl font-bold text-accent">
          Dataset Explorer
        </h1>
        <p className="mt-4 text-muted">Scaffold — built by the Explorer agent.</p>
      </div>
    </main>
  );
}

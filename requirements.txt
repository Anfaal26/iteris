# Iteris — minimal dependencies (compatible with Kaggle's default environment).
#
# Everything Kaggle already pre-installs (numpy, torch, scipy, pandas, matplotlib,
# tqdm) is intentionally NOT pinned — let Kaggle's defaults win to avoid pip
# resolver conflicts. We only list packages we actively need that may be missing
# or outdated.

monai>=1.3
nibabel>=5.0
SimpleITK
pyyaml
# scikit-image: required by the contour-refinement env (skimage.draw / skimage.measure)
# used by the TD3 contour agent. Pre-installed on Kaggle, but listed explicitly so
# the package also works on Colab / local envs where it may be missing.
scikit-image>=0.19
